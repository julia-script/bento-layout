node_modules/
dist/
docs/.next/
# Alternate build dirs (NEXT_DIST_DIR), used when a second dev server has to
# run against this checkout — `next dev` locks its distDir against reuse.
docs/.next-*/
docs/.source/
docs/next-env.d.ts
*.tsbuildinfo

.history/
.vercel
.env*

# Fuzz batches are regenerable (`pnpm fuzz-batch`) and run to several MB. A
# batch is a local working target, not a shared artifact — the findings that
# matter get promoted to fixtures under tests/fixtures/fuzz-found/ instead.
tests/fuzz-batches/
docs/public/monaco/
