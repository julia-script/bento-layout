---
title: Introduction
description: A TypeScript port of Taffy's flexbox, grid, and block layout algorithms.
---

import { SmokeTest } from '@/components/smoke-test';

Docs content goes here.

## Alias smoke test

The box below is laid out by the engine in `../src`, imported as
`bento-layout`, running at build time:

<SmokeTest />

If two equal boxes render side by side, the alias works.
